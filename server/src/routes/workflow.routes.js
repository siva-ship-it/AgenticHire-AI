import { Router } from 'express'; import { z } from 'zod'; import { requireAuth } from '../middleware/auth.js'; import { validate } from '../middleware/validate.js'; import { asyncHandler } from '../utils/api.js'; import { approvalSchema, workflowIdSchema } from '../validators/schemas.js'; import { approve, getWorkflow, listWorkflows, retry, start } from '../controllers/workflow.controller.js';
export const workflowRouter = Router(); workflowRouter.use(requireAuth); workflowRouter.get('/', asyncHandler(listWorkflows)); workflowRouter.post('/start', validate(z.object({ candidateId: z.string().length(24) })), asyncHandler(start)); workflowRouter.post('/retry', validate(workflowIdSchema), asyncHandler(retry)); workflowRouter.post('/approve', validate(approvalSchema), asyncHandler(approve)); workflowRouter.get('/:id', asyncHandler(getWorkflow));
