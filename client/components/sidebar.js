'use client';
import Link from 'next/link'; import { usePathname, useRouter } from 'next/navigation'; import { BriefcaseBusiness, ChartNoAxesCombined, LayoutDashboard, LogOut, Network, Users } from 'lucide-react'; import { useAuthStore } from '@/store/auth';
const links = [{ href: '/dashboard', label: 'Overview', icon: LayoutDashboard }, { href: '/dashboard/jobs', label: 'Jobs', icon: BriefcaseBusiness }, { href: '/dashboard/candidates', label: 'Candidates', icon: Users }, { href: '/dashboard/workflows', label: 'Workflows', icon: Network }, { href: '/dashboard/analytics', label: 'Analytics', icon: ChartNoAxesCombined }];
export function Sidebar() { const path = usePathname(); const router = useRouter(); const { user, logout } = useAuthStore(); return <aside className="w-64 bg-[#17211b] text-white min-h-screen p-5 flex flex-col fixed"><Link href="/dashboard" className="text-xl font-black tracking-tight mb-9">AgenticHire<span className="text-green-400"> AI</span></Link><nav className="space-y-1">{links.map(({ href, label, icon: Icon }) => <Link key={href} href={href} className={`flex items-center gap-3 px-3 py-2.5 rounded-lg text-sm ${path === href ? 'bg-green-700' : 'text-gray-300 hover:bg-white/10'}`}><Icon size={18}/>{label}</Link>)}</nav><div className="mt-auto border-t border-white/10 pt-4"><p className="text-sm font-semibold">{user?.name}</p><p className="text-xs text-gray-400 mb-3">{user?.email}</p><button className="flex items-center gap-2 text-sm text-gray-300" onClick={() => { logout(); router.push('/login'); }}><LogOut size={16}/> Sign out</button></div></aside>; }
