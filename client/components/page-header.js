export function PageHeader({ eyebrow = 'Recruitment operations', title, description, actions }) { return <header className="flex justify-between items-end gap-6 mb-8"><div><p className="uppercase tracking-[.18em] text-xs font-bold text-green-700 mb-2">{eyebrow}</p><h1 className="text-3xl font-black tracking-tight">{title}</h1>{description && <p className="text-gray-500 mt-2">{description}</p>}</div>{actions}</header>; }
